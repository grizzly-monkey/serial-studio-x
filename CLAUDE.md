# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`modbus-storm` is a Modbus client application. The only dependency currently installed is `@types/electron`, indicating an Electron-based desktop app is planned.

## Current State

This is a greenfield project — no source files, no build system, no test runner, and no linting tooling exist yet. When these are added, update this file with the relevant commands.
